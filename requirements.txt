# Core dependencies (standard library only for minimal footprint)
# pyyaml is the only external dependency for YAML parsing
PyYAML>=6.0

# LSP support for code analysis (v2)
pygls>=1.3.0
tree-sitter>=0.21.0
tree-sitter-python>=0.21.0
tree-sitter-javascript>=0.21.0
tree-sitter-typescript>=0.21.0

# Enhanced pattern recognition
numpy>=1.24.0
scikit-learn>=1.3.0

# Development dependencies
pytest>=7.0
